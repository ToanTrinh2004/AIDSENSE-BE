name: Test AidSense SSH

on:
  workflow_dispatch:

jobs:
  test-ssh:
    runs-on: ubuntu-latest

    steps:
      - name: Show SSH target
        run: |
          echo "================================"
          echo "SSH Target"
          echo "================================"
          echo "Testing host: 3.104.38.78"
          echo "Testing user: ubuntu"
          echo "Port: 22"

      - name: Prepare SSH key
        shell: bash
        run: |
          echo "================================"
          echo "Preparing SSH key"
          echo "================================"

          printf '%s\n' '${{ secrets.EC2_SSH_KEY }}' > "$RUNNER_TEMP/ec2_key"

          chmod 600 "$RUNNER_TEMP/ec2_key"

          echo "SSH key fingerprint:"
          ssh-keygen -lf "$RUNNER_TEMP/ec2_key"

      - name: Test network connection
        shell: bash
        run: |
          echo "================================"
          echo "Testing network connection"
          echo "================================"

          nc -vz -w 10 3.104.38.78 22

      - name: Test SSH connection
        shell: bash
        run: |
          echo "================================"
          echo "Testing SSH connection"
          echo "================================"

          ssh \
            -o StrictHostKeyChecking=no \
            -o ConnectTimeout=10 \
            -o PreferredAuthentications=publickey \
            -o PubkeyAuthentication=yes \
            -i "$RUNNER_TEMP/ec2_key" \
            ubuntu@3.104.38.78 \
            "echo 'SSH_CONNECTION_SUCCESS'; whoami; hostname; pwd"

      - name: SSH test completed
        run: |
          echo "================================"
          echo "SSH TEST PASSED"
          echo "================================"
